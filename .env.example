# Claude Clicky Keys Configuration
# ==================================

# Sound file to use (relative to sounds/ directory or absolute path)
# Available sounds in sounds/ folder:
#   - clicking-keys.mp3 (default)
# Add your own sound files to the sounds/ folder and reference them here
CLICKY_SOUND_FILE="clicking-keys.mp3"

# Volume control (0.0 to 1.0)
# 0.0 = silent, 0.5 = half volume, 1.0 = full volume
CLICKY_VOLUME="0.05"

# Maximum duration in seconds before auto-stop (safety feature)
# Prevents orphaned processes if stop signal is missed
CLICKY_MAX_DURATION="60"

# Playback speed (optional, 0.5 = half speed, 2.0 = double speed)
# Leave empty for normal speed
CLICKY_SPEED="1.0"
